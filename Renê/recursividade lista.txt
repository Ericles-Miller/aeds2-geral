#include <stdio.h>
#include <stdlib.h>

int binary( int n){

    if (n==1){
        printf("1");
        return 1;

    }
    else if ( n==0){
        printf("0");
        return 1;
    }
    else{
        binary(n/2);
        printf("%d", n%2);
    }
}

int mult_plus (int param, int mult){
    if (param>0)
    return mult+mult_plus( param-1, mult);
}
int soma_incemento( int n, int m){
  m++;
  if(n>0)
    soma_incemento(n-1, m);  
  
   return m;
   
}
